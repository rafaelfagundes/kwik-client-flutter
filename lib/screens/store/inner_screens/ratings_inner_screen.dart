import 'package:flutter/material.dart';

class RatingsInner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('RatingsInner'),
    );
  }
}
