import 'package:flutter/material.dart';

class ProductsInner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ProductsInner'),
    );
  }
}
